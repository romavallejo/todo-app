import { SafeAreaView } from "react-native-safe-area-context";

const RegisterUser = () => {
    return (
        <SafeAreaView>
            Login
        </SafeAreaView>
    );
};

export default RegisterUser;